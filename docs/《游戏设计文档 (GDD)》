# 游戏设计文档 (GDD) - V1.11

- **交付人:** GD-01 (游戏策划导师)
- **交付版本:** 1.11
- **核心风格:** 像素、夸张、搞笑、可爱

---

### **1. 核心玩法循环 (Core Gameplay Loop)**

游戏的核心循环已经明确并实现。玩家将体验以下流程：
1.  **选择钓点**: 从主菜单进入钓鱼场景，选择一个钓点（如小溪、森林湖）。
2.  **抛竿蓄力**: 进行一个考验时机和反应的蓄力小游戏，以决定抛竿类型（普通或完美）。
3.  **等待咬钩**: 鱼漂入水，经过一段随机时间后，触发咬钩事件。
4.  **提线反应**: 玩家必须在限定时间内点击，成功提线后进入核心博弈环节。
5.  **与鱼博弈**: 玩家通过控制一个“博弈条”追赶游动的“渔获图标”，填充进度条。
6.  **获得渔获**: 成功填满进度条则钓起渔获；进度条清空则失败。渔获包括各种鱼、垃圾，并有几率额外获得宝箱。
7.  **处理渔获**: 玩家可以将钓到的鱼放入鱼箱。
8.  **出售变现**: 在鱼箱界面，玩家可以一键出售所有渔获，换取金币，完成经济闭环。

### **2. 钓鱼核心机制详解**

钓鱼流程被清晰地划分为两个主要阶段。

#### **阶段一: 抛竿与提线 (Casting & Hooking)**

这个阶段决定了玩家是否有机会进入核心的“博弈”环节。

1.  **准备抛竿 (`ReadyToCast`)**: 玩家点击鼠标左键，开始蓄力。
2.  **蓄力 (`Casting`)**:
    * 一个带有“甜蜜点”区域的蓄力条出现。
    * “甜蜜点”会以固定速度左右移动，其宽度可在编辑器内配置。
    * 蓄力条的进度会根据一个可编辑的 `AnimationCurve` (抛物线速度模型) 非线性增长。
    * 玩家松开鼠标左键时，蓄力停止。如果最终蓄力值落在“甜蜜点”范围内，则判定为**“完美抛竿” (`Perfect Cast`)**；否则为“普通抛竿” (`Normal Cast`)。
3.  **等待咬钩 (`WaitingForBite`)**:
    * 系统会等待一个随机时长（默认为 1.5 到 4.0 秒）。
4.  **提线 (`Hooking`)**:
    * 等待结束后，一个巨大的“感叹号”图标出现，并开始快速缩小和消失。
    * 玩家必须在图标完全消失前点击鼠标。
    * **反应时间窗口**：普通抛竿为 0.5 秒，完美抛竿则延长至 1.5 秒，大大增加成功率。
    * 成功则进入“与鱼博弈”阶段；失败则鱼会跑掉，玩家需要重新开始抛竿。

#### **阶段二: 与鱼博弈 (The Mini-Game)**

这是模仿《星露谷物语》的核心玩法，考验玩家的持续操作能力。

1.  **界面元素**:
    * **玩家博弈条**: 玩家通过按住或松开鼠标左键来控制其上下移动。按住时匀速上升，松开时受重力影响下降。
    * **渔获图标**: 代表当前上钩的物品（鱼或垃圾），它会根据自身独特的行为逻辑上下移动。
    * **进度条**: 核心的胜负判断依据。
2.  **核心规则**:
    * 当“玩家博弈条”与“渔获图标”**重叠**时，进度条会以 `progressIncreaseRate` 的速率上涨。
    * 当两者**未重叠**时，进度条会以 `progressDecreaseRate` 的速率下降。
3.  **胜负条件**:
    * 进度条涨满 (`>= 1.0`)，玩家**成功**钓起渔获。
    * 进度条清空 (`<= 0.0`)，渔获**逃脱**，玩家失败。

### **3. 渔获系统 (Catchables System)**

游戏中的所有可钓物品（鱼、垃圾、宝箱）都构建在一个统一且高度可扩展的架构之上。

#### **3.1 数据结构**

* **`CatchableData` (可捕捉物数据)**: 这是一个抽象的基类 (`ScriptableObject`)，定义了所有可钓物品的通用属性，包括：
    * **核心信息**: 物品名称、图标、描述、基础售价。
    * **产出权重 (`BaseWeight`)**: 决定了此物品在钓鱼点奖池中被抽中的概率。
    * **分类 (`ItemCategory`)**: 如鱼、垃圾、材料等。
    * **博弈行为参数**:
        * `InitialNormalizedPosition`: 在博弈开始时，渔获图标出现的初始高度位置（0为底，1为顶）。
        * `BaseMoveSpeed`: 基础移动速度。
        * `StruggleThreshold`: 进度条超过此阈值后，渔获会切换到更激烈的“挣扎”行为模式。
        * `CalmBehaviorSequence` & `StruggleBehaviorSequence`: 两个行为列表，分别定义了渔获在“冷静”和“挣扎”状态下的具体动作序列。
* **派生类**:
    * **`FishData` (鱼类数据)**: 继承自 `CatchableData`，并添加了鱼的专属属性，如稀有度、出没地点、时间、以及与四种品质对应的不同长度范围。
    * **`TrashData` (垃圾数据)**: 同样继承自 `CatchableData`，用于定义各种垃圾的行为和属性。
    * **`TreasureChestData` (宝箱数据)**: 继承自 `CatchableData`，额外定义了作为“额外奖励”的出现概率 (`DropChance`)、宝箱自身的战利品池 (`LootPool`) 和可开出物品的数量范围。

#### **3.2 渔获行为系统**

渔获的行为不再是模糊的“四套逻辑”，而是通过一个**行为指令序列 (Action Sequence)** 的模式实现，具备极高的灵活性和可配置性。

策划可以在 `FishData` 或 `TrashData` 中，像搭积木一样组合以下已实现的**行为原子 (`FishAction`)**：

* **`Move_Action` (移动)**: 使渔获平滑移动到一个随机或预定目标点。
    * **参数**: 移动次数、单次移动最大时长、偏好区域、回归偏好区域的几率 (`HomingBias`)。
* **`Wait_Action` (等待)**: 使渔获在原地停顿一段时间。
    * **参数**: 等待时长。
* **`Jump_Action` (瞬移)**: 使渔获在短暂停顿后，瞬间出现在一个新的随机位置。
* **`ChangeSpeed_Action` (改变速度)**: 瞬间改变渔获的 `CurrentSpeed`。
    * **参数**: 新的速度值。
* **`Jitter_Action` (抖动)**: 在持续时间内，使渔获在一个范围内进行快速、无停顿的上下往复移动。
    * **参数**: 总持续时间、每次移动的最小/最大距离。

#### **3.3 产出与奖池机制 (Loot Pool)**

* 每个**`FishingSpot` (钓鱼点)** 脚本都管理着一个独立的奖池 `lootPool`。
* 当玩家开始钓鱼时，系统会调用 `SelectItemByWeight` 方法。该方法会计算奖池内所有物品的 `BaseWeight` 总和，然后生成一个随机数，根据权重区间来决定具体钓上哪一种 `CatchableData`。这是一个非常经典的**权重随机**算法。

### **4. 奖励与品质系统**

#### **4.1 品质系统 (Quality)**

* **品质等级**: 已实现四种品质等级：**吹牛资本 (最好)、史诗对决、像模像样、勉强上钩 (最差)**。
* **判定机制**: 品质完全由玩家在“与鱼博弈”环节中的表现决定。系统会记录“玩家博弈条”与“渔获图标”**从重叠变为不重叠**的次数 (`_progressDropCount`)。
    * **0 次**: 吹牛资本
    * **1 次**: 史诗对决
    * **2 次**: 像模像样
    * **3 次及以上**: 勉强上钩

#### **4.2 长度系统 (Length)**

* 鱼的最终长度与品质直接挂钩。
* 每种 `FishData` 都定义了分别对应四种品质的长度范围（如 `LengthRangeChuiNiuZiBen`）。
* 成功钓上后，系统会根据最终获得的品质，在对应的长度范围内随机生成一个最终长度值。

#### **4.3 宝箱系统 (Treasure Chest)**

* 宝箱是成功钓上鱼后的**额外奖励**，并非通过博弈小游戏直接获得。
* 每次成功钓上鱼后，系统会遍历所有宝箱数据 (`TreasureChestData`)，根据其 `DropChance` 参数进行一次独立的随机判定。
* 如果判定成功，玩家会看到一个开箱动画序列，并获得宝箱 `LootPool` 中的随机物品。

### **5. 经济与库存系统**

#### **5.1 经济闭环**

* **赚钱**: 玩家通过钓鱼获得渔获。在鱼箱界面，可以将所有渔获一键出售，其总价值会转化为金币。
* **价值计算**:
    * 普通物品 (如垃圾) 的售价为其 `BasePrice`。
    * 鱼的售价会基于 `BasePrice` 乘以一个**品质系数**，品质越高，售价越高。
        * 吹牛资本: x2.0
        * 史诗对决: x1.5
        * 像模像样: x1.0
        * 勉强上钩: x0.8
* **花钱**: 原始 GDD 中提到的升级渔具等金币消耗途径，在当前代码版本中尚未实现 (`ShopUIController` 等脚本为空)。

#### **5.2 库存系统 (Inventory)**

* 系统通过 `InventoryManager` 统一管理所有物品。
* **独特物品 vs. 可堆叠物品**:
    * **鱼 (`FishInventoryItem`)**: 被视为独特物品。每一条成功钓上的鱼都是一个独立的实例，拥有自己独一无二的品质和长度。在库存中，它们以 GUID 作为唯一标识符，**不会堆叠**。
    * **垃圾等其他物品 (`InventoryItem`)**: 被视为可堆叠物品。同一种垃圾（以其 `ScriptableObject` 的名字作为标识符）如果被多次钓上，其数量 (`quantity`) 会累加，而不会创建新条目。

### **6. 游戏流程与场景**

* **主流程控制器**: `GameFlowManager` 负责管理核心UI面板的切换，如主菜单 (`MainMenuPanel`) 和钓鱼场景 (`FishingUIPanel`)。
* **已实现钓点**: 代码中已包含并可选择三个钓鱼点：
    * **小溪 (Creek)**
    * **森林湖 (Forest Lake)**
    * **沙滩 (Beach)**
    * 注意：原始 GDD 中的“公厕马桶”未在当前代码中实现。

### **7. V1.11 版本未实现功能**

根据代码分析，以下在原始 GDD 中提到的系统在当前版本中**尚未实现**，大部分相关脚本仅为占位符：

* **渔具系统 (Equipment System)**: 鱼竿升级、鱼箱容量、鱼饵等功能均未实现。`EquipmentManager` 为空脚本。
* **鱼网系统 (Offline Reward System)**: 无相关代码。
* **体力系统 (Stamina System)**: 无相关代码。
* **任务系统 (Quest System)**: 仅在 GDD 中提及，无相关代码。
* **图鉴系统 (Collection/Pokedex)**: 仅在 GDD 中提及，无相关代码。
* **商店系统 (Shop System)**: `ShopUIController` 和 `ShopManager` 为空脚本，无实际功能。
* **家园场景 (Home Scene)**: 仅在 GDD 中提及，无相关代码。
* **材料系统 (Material System)**: 尽管 `ItemCategory` 中有 `Material` 枚举，但目前宝箱和垃圾掉落的仍是 `CatchableData`，没有独立的材料掉落和合成逻辑。

---
> **GD-01 总结:**
>
> V1.11 版本已经构建了一个非常扎实和可玩的核心钓鱼循环。数据驱动的渔获系统和行为组合机制为未来的内容扩展打下了坚实的基础。接下来的工作重点应放在完善经济循环的后半环（金币消耗），并逐步实现渔具、任务等外围系统，以丰富游戏的中长期目标。